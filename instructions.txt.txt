In order to runserver, please install the following with mentioned cli

1. Pillow
	pip install Pillow

2. Rest Framework
	pip install djangorestframework
	pip install markdown
	pip install django-filter